comment: Long-poll normalized stream events from a running agent process.
↓Run [steps.SimpleStep-1781600000401]: 
  expression: |
    'include("js/vibe_agent_bridge.js");
    result = C8O.agentBridge.events({
      handle: typeof handle === "undefined" ? "" : handle,
      cursor: typeof cursor === "undefined" ? "" : cursor,
      limit: typeof limit === "undefined" ? "" : limit,
      waitMs: typeof waitMs === "undefined" ? "" : waitMs
    });'
↓result [steps.JsonToXmlStep-1781600000402]: 
  jsonObject: 
    - xmlizable: 
      - ↑classname: com.twinsoft.convertigo.beans.steps.SmartType
      - SmartType: 
        - ↑mode: JS
        - →→: result
  key: 
    - xmlizable: 
      - ↑classname: com.twinsoft.convertigo.beans.steps.SmartType
      - SmartType: 
        - ↑mode: PLAIN
        - →→: result
↓handle [variables.RequestableVariable-1781600000403]: 
  comment: Process handle returned by agent_vibe_start. Defaults to the handle stored in the HTTP session.
  description: Process handle returned by agent_vibe_start. Defaults to the handle stored in the HTTP session.
↓cursor [variables.RequestableVariable-1781600000404]: 
  comment: Cursor returned by the previous agent_events call. Defaults to 0.
  description: Cursor returned by the previous agent_events call. Defaults to 0.
↓limit [variables.RequestableVariable-1781600000405]: 
  comment: Maximum number of events to return. Defaults to 100.
  description: Maximum number of events to return. Defaults to 100.
↓waitMs [variables.RequestableVariable-1781600000406]: 
  comment: Long-poll wait when no event is available. Defaults to 25000, capped at 30000.
  description: Long-poll wait when no event is available. Defaults to 25000, capped at 30000.